<% if !flash.empty? %>
  <div class='row no-margins'>
    <div class='col-md-6 col-md-offset-3'>
      <% flash.each do |key, value| %>
        <%= content_tag(:div, value, class: "margin-top-10 alert alert-#{flash_style_for(key)}") %>
      <% end %>
    </div>
  </div>
<% end %>